"use client";

import Image from "next/image";
import styles from "../../public/css/login.module.css";

export default function Login() {
    return (
        <body className={styles["auth-page"]}>
            <main className={styles["auth-card"]} id={styles["auth"]}>
                asdadasd
            </main>
        </body>
    )
}